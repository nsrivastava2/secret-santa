# ===========================================
# Secret Santa - Environment Configuration
# ===========================================

# Database
DATABASE_URL="file:./data/secret-santa.db"

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here-generate-with-openssl-rand-base64-32

# ===========================================
# OAuth Providers (configure at least one)
# ===========================================

# Google OAuth (for Gmail and Google Workspace accounts)
# Get credentials at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Microsoft/Azure AD OAuth (for Outlook.com, Microsoft 365, and Exchange accounts)
# Get credentials at: https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps
AZURE_AD_CLIENT_ID=
AZURE_AD_CLIENT_SECRET=
# Use "common" for any Microsoft account, or your tenant ID for organization-only
AZURE_AD_TENANT_ID=common

# ===========================================
# Email Configuration (Optional - can be configured in Settings UI)
# ===========================================
# These are fallback values if not configured in the Settings page

# SMTP Settings
EMAIL_SERVER=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASSWORD=

# Email addresses
EMAIL_FROM=secret-santa@yourcompany.com
HR_EMAIL=hr@yourcompany.com

# ===========================================
# Admin Configuration (Optional - can be configured in Settings UI)
# ===========================================
# First user to sign in becomes admin if not specified
# ADMIN_EMAIL=admin@yourcompany.com

# ===========================================
# Notes:
# ===========================================
# 1. Most settings can be configured via the web-based Settings page after first login
# 2. The first user to log in will be redirected to a setup wizard
# 3. For Gmail SMTP, use an App Password (not your regular password)
# 4. For Microsoft 365 SMTP, enable SMTP AUTH in your admin settings
